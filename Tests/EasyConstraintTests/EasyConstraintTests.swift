import XCTest
@testable import EasyConstraint

final class EasyConstraintTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(EasyConstraint().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
